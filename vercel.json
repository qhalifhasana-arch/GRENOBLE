{
  "version": 2,
  "framework": null,
  "installCommand": "npm install --include=dev",
  "buildCommand": "npx vite build && npx esbuild api/index.ts --bundle --platform=node --target=node18 --format=cjs --outfile=api/bundle.js --alias:@shared=./shared --external:pg-native --external:@mapbox/node-pre-gyp --external:bcrypt",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/bundle" },
    { "source": "/(.*)",       "destination": "/index.html" }
  ],
  "functions": {
    "api/bundle.js": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
}
